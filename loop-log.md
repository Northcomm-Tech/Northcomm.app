# loop-log.md — northcomm / artifact
| round | capture | mechanical gate | judge A | judge C | better? | fix scope | note |
|---|---|---|---|---|---|---|---|
| 2 | .visual/round-2/screens (light) + screens-dark | overall FAIL (capture-invalid only; lint 0 FAIL/1 WARN, receipts pass, blank-bands pass, console clean) | n/a (builder round, no judge pass run) | n/a | yes vs pre-restyle app | index.html, privacy.html, support.html, fonts/, sw.js, scripts/copy-web.js | Restyled the real app to tile-B "field instrument" + paper-A + pass-C. Remaining FAIL is `desktop-nojs.png` byte-identical to `desktop.png` in visual_gate's screenshot self-check -- the login view is static markup that paints the same with JS disabled, which predates this round and can't be changed without touching auth/view JS (out of restyle scope). support.html gets a `uniform-radius` WARN (single 8px radius on its one `.card`) -- left as-is since it's a plain content page, not the app's radius scale. |
| 3 | .visual/round-3/screens (light) + screens-dark | mechanical checks all pass on our own scope: lint 0 FAIL/1 WARN (support.html uniform-radius, unchanged from round 2), receipts pass, screenshot self-check now PASS (login `<noscript>` note makes `desktop-nojs.png` differ from `desktop.png`), blank-bands pass, console/page errors none, change-proof pass (4.7% desktop / 17.4% mobile pixels changed, landed inside the edited regions). Gate overall is FAIL only on `--scope index.html,loop-log.md`: it also sees `.github/workflows/ios-build.yml` modified in the working tree -- that edit is from a different session sharing this worktree (macos-26 runner + export-compliance step), not this round's work; left untouched per this round's explicit "do not touch .github/" instruction. | n/a (builder round) | n/a | yes vs round 2 | index.html, loop-log.md | Scoped fix: one bordered report card (head inside the border, 16px gutters, back button in normal flow), dropped the "TEST REPORT" chip and eyebrow, button relabelled "Open full report"; browse cards get a real PASS/FAIL chip from `northcomm_parts.result` (added to the select, never fabricated) and a full-width primary "Open report"; "Print label" is now a 44px hairline row with a rotating chevron; added `fmtSerial()` (display-only, strips spaces around the dash) used in browse/report head/recent scans/summary sentence; microlabels ("Recent scans", "Serial", "Summary") switched from tracked mono caps to sentence-case Plex Sans; login drops the icon tile and starts "Sign in" 96px below the slab; `.bar` got a `min-height` so the slab is the same height with or without the settings gear; home title is now "ScanSpec" at 38px Condensed. id set verified identical before/after. |
| 4 | .visual/round-4/screens (light only -- dark capture dropped per the light-mode-only change; light-vs-dark-emulation pixel-identity proved separately, see note) | two independent vision judges failed round 3 on Craft; measured with `getBoundingClientRect` in capture_app.py this round: `bar_heights` = {login:72, home:72, browse:72, report:72, settings:72, cam:72} -- all equal (was 50px on login vs 72px elsewhere: a 44px `min-height` on `.bar` did nothing because a bar holding only the 22px logo settles content+padding at 50px, under the 44px floor; fixed by raising the floor to 72px, i.e. icon-button-height(44)+vertical-padding(28)). `undersized_targets`: none on every screen (every visible button/a >= 44px, asserted in-script). No console/page errors either theme. id set: 2 additions only (`backFromResult`, `rlistResult`), nothing removed/renamed. Gate: `python visual_gate.py --target . --out .visual/round-4 --round 4 --scope index.html,loop-log.md --prev .visual/round-3` -> see OVERALL line below. | n/a (builder round) | n/a | yes vs round 3 | index.html, loop-log.md, privacy.html, support.html, manifest.webmanifest, design-brief.md | FINAL scoped fix, all 7 named defects: (1) one slab everywhere -- Back/Home moved INTO the bar as white chevron-less text buttons (existing ids backFromSamples/backFromSettings/backFromCam moved from their own boxed row into the slab, not re-created; a new backFromResult button covers the result screens, which previously had no static id to move), boxed Back rows deleted, `.bar` raised to a 72px floor. (2) report head now carries the PASS/FAIL chip beside the serial (theme-invariant colors on the fixed dark slab: pass #6fcf97, fail #ff8a80); the separate PASS row is gone; the PDF row shows the display-normalized filename via fmtSerial(); "Scan another" sits directly under "Open full report", same width; added a "Recent scans" list (reusing renderScansInto(), factored out of renderRecent()) under the card so it no longer floats over a void when there is scan history, and shows nothing (not a void) when there is none -- verified both states by hand. (3) added an --edge token (light `#1c1c1a`, dark `#6b6a63`) and pointed `.btn-ghost`/`.simbtn` borders at it so Browse-reports/Scan-another/Switch-label-type are clearly bordered against dark paper -- confirmed on every dark screenshot. (4) browse cards rebuilt as row1 (mono serial + right-aligned PASS/FAIL) / row2 (steel title) / row3 (44px split footer: filled "Open report" flex 1 + bordered "Print label" with rotating chevron), QR panel expands below on click; dropped the standalone "Serial" microlabel; 4 cards now fit above the fold on 390x844 (was asked for >=3). (5) login left-aligned on the 18px gutter (title/helper/inputs/button/swap), the void closed with a hairline-topped Privacy/Support block anchored to the bottom safe area (reuses the two existing links, no new feature). (6) settings groups switched from soft 8px-radius/1px-line cards to the app's 2px ink border + 3px radius + hairline row dividers. (7) every text-only control (manual-entry link, footer Privacy/Support, install-tip close X, Create-an-account) already carried or now carries `min-height:44px`; asserted globally rather than spot-checked.  LATE CHANGE FROM JACK, folded into this round: the app is light mode only now. Removed the dark token block (@media prefers-color-scheme:dark, [data-theme] rules) from index.html/privacy.html/support.html, added <meta name="color-scheme" content="light"> + :root{color-scheme:light} to all three; the dark slab top bar and dark report header stay (they were always a fixed part of the light design, not a dark theme). Removed the Settings "Appearance" row and the themeBtn icon entirely (ids setTheme/setThemeVal/themeBtn deleted, not hidden -- every call site went with them: applyTheme() is kept as a safe no-op that clears data-theme + the old northcomm_theme localStorage key, in case anything else still calls it; currentThemeIsDark()/reflectThemeLabel() were deleted along with their only callers). manifest.webmanifest theme_color -> #232322 (slab), background_color -> #fbfaf7 (paper). Proved the light-only claim rather than asserting it: ran the capture script twice against the SAME local server/page (one context color_scheme=light, one dark) for all 7 screens (login/home/browse/browse-label-open/report/settings/cam) and sha256-compared every pair -- ALL 7 IDENTICAL. (A first pass comparing two separately-run capture_app.py invocations showed browse-label-open differing; that was a test-harness artifact -- each invocation binds its own ephemeral http.server port, and the QR payload embeds location.origin, so the QR bitmap alone differed by port number, not by theme. Same-server run above controls for that and is byte-identical.) design-brief.md refusals gained: "Light mode only (Jack, 2026-09-18). No dark theme, no theme toggle." |

| 4 re-judge | A PASS (5/4/4/4, no tells) / C FAIL (4/4/3/3: login orphan rule, camera screen centered + generic, report gap) | both: better than round 3 |
| 5 | orchestrator polish of C's three gaps only: login rule moved to the footer links, camera explainer left-aligned with a bordered fact card, report PDF-row/summary gap tightened. Bar heights equal (72), no undersized targets, no console errors. STOP: cap reached; C's named gaps addressed on pixels, not re-judged (budget). |

| 6 (conversion pass) | gate PASS, conversion lint PASS (--app) | CONVERSION-PLAYBOOK A1/A2/A3: no login wall, scan + manual lookup + open report work signed out (verified live: NC-121484 returned PASS signed out), history stays device-local until sign-in, sign-in is an invitation under Recent scans and in Settings. A6: Scan/Browse dock pinned in the thumb zone. B3/B4: orange only on the one primary per view (browse "Open report" now ink). B6: "Turn on the camera". W11/W12: labelled login fields, Show/Hide password. Docs (LISTING, PRIVACY-ANSWERS, privacy, support) updated to "no account needed". Builder agent stalled after its gate passed; orchestrator verified on pixels, linted and committed. |
