# Northcomm ScanSpec - Screenshots Still Needed

These are NOT yet produced. Capture them from the live app
(https://northcomm-tech.github.io/Northcomm.app/) in a phone-sized browser or on device,
then drop the exported PNGs into App Store Connect.

## Required sizes
- **6.7" display: 1290 x 2796 px** (portrait) - required
- **6.5" display: 1242 x 2688 px** (portrait) - required
- **5.5" display: 1242 x 2208 px** (portrait) - optional now; skip unless time allows

Provide 3 to 5 screenshots per required size. Same set of shots, re-exported at each size.

## Shots to capture (in order)

1. **Home / scan screen** - the camera scan view as the app opens, framed on a QR label.
   This is the hero shot; make it the first screenshot. Add a short caption overlay like
   "Scan the label on any Northcomm assembly."

2. **Resolved report card** - the result after a successful scan, showing the assembly's
   factory test report / spec sheet for a real serial. Caption: "Instant factory test
   report and spec sheet."

3. **QR sample** - the sample QR code screen (the one a reviewer can scan to test).
   Caption: "Works with the QR label printed on every assembly." This doubles as the
   reviewer test aid referenced in LISTING.md.

4. **Sign-in screen** - the optional email/password sign-in. Caption: "Optional sign-in
   saves your scan history across devices."

5. (Optional) **Scan history list** - the saved history view when signed in, if it makes
   a clean shot. Caption: "Your recent lookups, synced."

## Notes
- Keep captions honest and benefit-led; no em dashes.
- Use real assembly data (a real serial that resolves to a real PDF), not lorem ipsum.
- Status bar should look clean (full battery, no notifications) if captured on device.
- All screenshots come from the live app; no mockups needed.
